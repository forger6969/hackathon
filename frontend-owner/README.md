# Frontend Owner

Экран хозяина "Сегодня". Смотри `/CLAUDE.md` в корне репозитория — там стек,
API-контракт и .env-настройка.

Быстрый старт:

```
npm create vite@latest . -- --template vanilla
npm install socket.io-client
```
