# Frontend Master

Экран мастера: живая очередь, три кнопки статуса. Смотри `/CLAUDE.md` в корне
репозитория — там стек, API-контракт и .env-настройка.

Быстрый старт:

```
npm create vite@latest . -- --template vanilla
npm install socket.io-client
```
